<#if isWithPackage?exists && isWithPackage==true>package ${packageName}.infrastruture.helper;</#if>

/**
 * ${classInfo.classComment} - helper
 *
 * @author ${authorName}
 * @date ${.now?string('yyyy-MM-dd HH:mm')}
 */
public interface ${classInfo.className}Helper {



}
